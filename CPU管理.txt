CPU是如何工作：自动的取指--执行

管理CPU：设置好PC初值就OK了

一条IO指令消耗的时间很长，因为需要从磁盘读取数据

如果按顺序执行，CPU的效率会很低

一个程序执行到IO指令的时候，就切换到另一个程序执行，等IO指令执行完再切回来

多道程序、交替执行

一个CPU上交替的执行多个程序：并发

适当的时候控制寄存器的PC的切换、还需要有一个存放信息的结构：PCB

运行的程序和静态程序不一样！

进程是进行（执行）中的程序

多进程如何组织？
Process Control Block：用来记录进程信息的数据结构

启动磁盘读写；
pCur.state = 'W';
将pCur放到DiskWaitQueue;
schedule();

schedule()
{
    pNew = getNext(PreadyQueue);
    switch_to(pCur,pNew);
}

交替的三个部分：队列操作+调度+切换

多进程会引入一个问题，两个进程都访问了一个地址，这样会起冲突，这时候就引入了虚拟内存的概念。

进程同步

读写PCB，OS中最重要的的结构，贯穿始终
要操作寄存器完成切换
要写调度程序
要有进程同步与合作
要有地址映射

